---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: steve1145/debuging

params:
  POM_PATH:
  
inputs:
  - name: project
  - name: build
  - name: compile-out
  
#outputs:
#  - name: compile-out

run:
  path: build/tag/compile-exe.sh
